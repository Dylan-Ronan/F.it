# FIT-Backend
Backend for the capstone

Using node express, we have a backend server which will handle the storage and computations of the web app, the frontend will just be a dummy. We will not decrypt data as end-to-end encryption needs the frontend to decrypt locally.

To run this server
```
cd express-app
node server.js
```